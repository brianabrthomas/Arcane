# Arcane — Legal Alpha Exchange (backend)
# Python 3.11+ recommended

# --- Web framework / server ---
fastapi==0.136.3
uvicorn[standard]==0.48.0

# --- Data layer ---
SQLAlchemy==2.0.50
pydantic==2.13.4

# --- HTTP client (CourtListener ingestion, general) ---
httpx==0.28.1

# --- Agentic LLM layer (optional; only used when LLM_MODE=live) ---
anthropic==0.105.2

# --- Blockchain / payments (Arc + USDC + x402 / EIP-3009 signing) ---
eth-account==0.13.7
web3==7.16.0
